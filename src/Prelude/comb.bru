--!-- combinatory calculus

-- basic combinators
S        := λλλ (2 0 (1 0))
K, const := λλ1 
I, id    := λ0

-- fixed points
Ω := λ (0 0)
Y := λ ((0 Ω) (0 Ω))

-- non eta-reduced function application variants
etaI  := λ S{K, K, 0}
